/**
 * stackdiff env parser module
 *
 * Provides utilities for loading and parsing .env files
 * into structured key-value maps for diffing and reconciliation.
 */
export { parseEnvFile, parseEnvString } from './envParser';
export type { EnvMap } from './envParser';
