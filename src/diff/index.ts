/**
 * @module diff
 * Provides utilities for comparing environment variable maps and detecting differences.
 */
export { diffEnvMaps, hasDifferences } from './envDiff';
export type { EnvDiffResult, DiffEntry } from './envDiff';
